<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Material.Light">
        <item name="android:buttonStyle">@android:style/Widget.Material.Light.ActionButton.CloseMode</item>
    </style>

</resources>
